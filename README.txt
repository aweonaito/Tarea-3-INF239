-En el codigo se ve que posee partes para generar datos en la base de datos, estos eran para tener datos con los cuales trabajar e ir probando las implementaciones del CRUD.
-En la tabla de 'usuario_tiene_moneda' se agrego una columna nueva, ya que el codigo tiraba error al intentar crear una tabla sin una primary key, esta tabla nueva solo sirve para resolver ese problema.
-Las dos ultimas implementaciones del CRUD que se ven en el codigo no funcionan(tabla 'precio_moneda', implementaciones 'DELETE' y 'PUT'), se intento de varias maneras y  se pudo decifrar el como hacerlo.
-Para acceder a algo en espeficio se tiene que ingresar algo como argumento por el URL(Ej: buscar al usurio 11, /api/usuario/11).

-La pagina esta hecha sin tomar en cuenta la tarea 2 ya que esta no la terminamos y la teniamos mala, a vista tambien que no pudimos hacer que funcionara (seguimos intentando ahora mientras haciamos la tarea 3) hicimos la pagina de simulacro a parte de la pagina web de la tarea 2 por un tema de poder hacer algo.
-La interfaz no esta hecha, hay un un tipo de proto mainpage, para tener la tarea como en el ejemplo concentrado que habia que aparece en el pdf de la tarea, el cual nos lleva a unas interfaces para implementar las consultas, las cuales no sabemos por que no funcionan y por cosa de tiempo no pudimos arreglar, la idea hubiera sido el aregar lo demas para que quedara bien la interfaz, el agregar las tablas y la implementacion del CRUD.


PD: Somos el grupo 11, nosotros eramos un integrante menos, hablamos con un ayudante y nos dijo que teniamos que implementar 3 consultas y el CRUD, pero se nos olvido e intentamos hacer las interfaces de las 3 consultas que ibamos a usar y no supimos bien como implementar el CRUD en la pagina web tomando en cuenta que no nos alcanzo el tiempo.
